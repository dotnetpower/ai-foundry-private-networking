# =============================================================================
# AI Foundry Standard Agent Setup - 환경 설정
# =============================================================================
# 이 파일을 config.env로 복사하고 값을 수정하세요.
# cp config.env.example config.env

# -----------------------------------------------------------------------------
# 필수 설정
# -----------------------------------------------------------------------------
SUBSCRIPTION_ID=""                          # Azure 구독 ID
LOCATION="eastus"                            # Azure 리전 (eastus2에서 AI Search 리소스 부족으로 변경)
RESOURCE_GROUP_NAME="rg-aifoundry-agent"    # 리소스 그룹 이름

# -----------------------------------------------------------------------------
# VNet 설정
# -----------------------------------------------------------------------------
# VNet 리소스 그룹 (비어있으면 RESOURCE_GROUP_NAME 사용)
VNET_RESOURCE_GROUP=""                      # VNet 리소스 그룹 (비워두면 RESOURCE_GROUP_NAME 사용)

# VNet 설정
VNET_NAME="vnet-agent"                      # VNet 이름
VNET_PREFIX="10.0.0.0/16"                   # VNet 주소 공간

# 서브넷 설정
AGENT_SUBNET_NAME="agent-subnet"            # Agent 서브넷 이름
AGENT_SUBNET_PREFIX="10.0.0.0/24"           # Agent 서브넷 (최소 /26, 권장 /24)
PE_SUBNET_NAME="pe-subnet"                  # Private Endpoint 서브넷 이름
PE_SUBNET_PREFIX="10.0.1.0/24"              # PE 서브넷

# -----------------------------------------------------------------------------
# AI Services 설정
# -----------------------------------------------------------------------------
AI_SERVICES_NAME="aiservices"               # AI Services 이름 접두사
PROJECT_NAME="project"                      # Project 이름 접두사
PROJECT_DESCRIPTION="AI Foundry Agent Project with Private Network"
DISPLAY_NAME="Network Secured Agent Project"

# -----------------------------------------------------------------------------
# 리소스 이름 설정 (선택사항 - 비워두면 자동 생성)
# -----------------------------------------------------------------------------
# 예시: STORAGE_NAME_PREFIX="staiagent" + 랜덤4자리 → "staiagentab12"
#       비워두면: AI_SERVICES_NAME + 랜덤4자리 + "st" → "aiservicesab12st"
STORAGE_NAME_PREFIX="staiagent"             # Storage Account 이름 접두사 (3-15자, 소문자/숫자만)
COSMOSDB_NAME_PREFIX="cosmosagent"          # CosmosDB 이름 접두사 (예: cosmosagentab12)
AI_SEARCH_NAME_PREFIX="srchagent"           # AI Search 이름 접두사 (예: srchagentab12)
SEARCH_SKU="basic"                          # AI Search SKU (free, basic, standard, standard2, standard3)

# -----------------------------------------------------------------------------
# 모델 배포 설정
# -----------------------------------------------------------------------------
MODEL_NAME="gpt-4o"                         # 배포할 모델
MODEL_FORMAT="OpenAI"                       # 모델 형식
MODEL_VERSION="2024-11-20"                  # 모델 버전
MODEL_SKU_NAME="GlobalStandard"             # SKU 이름
MODEL_CAPACITY=30                           # TPM 용량 (1000 TPM 단위)

# -----------------------------------------------------------------------------
# 테스트 데이터 설정
# -----------------------------------------------------------------------------
TEST_DATA_CONTAINER="test-documents"        # Blob 컨테이너 이름
AI_SEARCH_INDEX_NAME="documents-index"      # AI Search 인덱스 이름

# -----------------------------------------------------------------------------
# 태그
# -----------------------------------------------------------------------------
TAG_ENVIRONMENT="Development"
TAG_PROJECT="AI-Foundry-Agent"
TAG_MANAGED_BY="Terraform"
